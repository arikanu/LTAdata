package TextUtils;

import java.io.File;
import java.nio.file.Path;
import java.nio.file.Paths;

public class StrFileNames {

	public static String getFileName(String pathname) {
		Path path = Paths.get(pathname);
		return path.getFileName().toString();
	}
}
