package XmlReader;

import org.w3c.dom.Document;
import org.w3c.dom.Element;
import org.w3c.dom.Node;
import org.w3c.dom.NodeList;

public class XmlUtils {

	public static NodeList getNodeListByTag(Document doc, String tag) {
		return doc.getElementsByTagName("entry");
	}
	
	public static NodeList getNodeListFromElementByTag() {
		
	}
	
	public static String getFirstElementOfNodeList(Node node, String tagChild) {
		if (fstNode.getNodeType() == Node.ELEMENT_NODE) {
			Element fstElmnt = (Element) node;
			
		}
		
		return "";
	}
}
